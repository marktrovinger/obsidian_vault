## Friday Nov 22
- `panda_gazebo` is what wraps `frank_ros` in order to RL agents, in order to use the `ros_gazebo_gym` package effectively, we will likely need to implement something similar, a `kinova_gazebo` which will wrap the `ros_kortex` package
- 