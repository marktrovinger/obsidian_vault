## Quiz 1
- $f(n) = \log n^2; g(n) = \log n + 5; f(n) = \Theta(g(n))$: True
- $f(n) = n; g(n) = \log n^2; f(n) = \Omega(g(n))$: True
- $f(n) = \log \log n; g(n) = \log n; f(n) = O(g(n))$: True
- $f(n) = n \log n; g(n) = \log^2 n; f(n) = \Omega(g(n))$: True
- $f(n) = n \log n + n; g(n) = \log n; f(n) = \Omega(g(n))$: True
- $f(n) = 10; g(n) = \log 10; f(n) = \Theta(g(n))$: True
- $f(n) = 2^n; g(n) = 3^n; f(n) = O(g(n))$: True
- $f(n) = n^2 \log n; g(n) = n^2; f(n) = O(g(n))$: False
- $f(n) = n!; g(n) = 2^n; f(n) = O(g(n))$: False

## Quiz 2
- For the _worst-case,_  Binary search has a better (lower) asymptotic bound than the Sequential search. True
- The asymptotic bound for the best-case for both Sequential search and Binary search is $\Theta(1)$. True
- The following expression of "n factorial" is expressed iteratively: $fact(n) = nfact(n-1)$ False
- The following expression of "n factorial" is expressed recursively: $n! = n(n-1)(n-2)...1$ False
- Both Mergesort and Quicksort are using a divide and conquer strategy for algorithm design. True
- Mergesort and Quicksort both have the same worst-case time complexity. False
- Mergesort and Quicksort have the same average-case complexity. True
- Merge Sort is a “stable sort” and faster than Quicksort for sorting linked lists. True
- Quicksort uses “in-place sorting”, and for sorting arrays, it has much better _space complexity_ than Merge Sort. True
- Quicksort _worst-case_ time complexity is $O(n\log n)$ and Merge Sort _worst-case_ time complexity is $O(n^2)$. False

## Quiz 3
- The amount of storage the adjacency matrix requires to store a graph is $O(V^2)$. True
- The amount of storage the adjacency list requires to store a graph is $O(V+E)$. True
- A graph has always only one minimum spanning tree. False
- Dijkstra’s algorithm is used for finding the minimum spanning tree. False
- Kruskal’s algorithm is used for finding the single source shortest paths from one vertex to all vertices in a graph. False
- Prim’s and Kruskal’s algorithms find the single source shortest paths from a vertex to all the vertices in a graph. False
- The Breadth First Search is used to find whether or not a graph is acyclic. False
- The Breadth First Search is used to find whether or not a graph is connected. False
- The Depth First Search is used to find the shortest paths in a graph. False
- Depth First Search is used to find various properties of a graph. True

